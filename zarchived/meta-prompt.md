@/prompt '/refine first create a folder called 'connect-da-dots/' then take @all-prompt.md and split this up into as many micro steps as possible. be obsessive with it. then turn each of those steps into individual prompts for claude code to execute in Sequential order all in fresh chats with a cleared contacts window. They should be self-contained with context, Update a json state file that the first prompt will create, Use that state file (or create as many state files as needed) For for cloud code and any sub agents that it spins off to ensure that it creates a context rich environment that fits the idea of many micro prompts as well as each of the microprompts spin off sub agents to get things done faster if it is going to contribute to better results accurate results etc. Each prompt will ensure that no sub agent or cloud code in itself will output more than 32,000 tokens in one go. Quad code has a 32,000 token output limit so this needs to be a hard constraint that never breaks. The reason behind splitting up this prompt into many micropoms and having sub agents is because this is a large and complex mobile application that I want to create so the purpose of it is to ensure that claude code approaches this three's prompts in the most effective way possible with no errors.'